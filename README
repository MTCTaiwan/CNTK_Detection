
```
pip install nvidia-docker-compose
nvidia-docker run --rm nvidia/cuda nvidia-smi
```

```
nvidia-docker-compose up -t docker-compose.yml.jinja --force-recreate predict-core* web-lb predict-lb
nvidia-docker-compose down -t docker-compose.yml.jinja
```

```
nvidia-docker-compose up web predict
```
